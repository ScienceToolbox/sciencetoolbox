.row
  .small-12.medium-5.large-5.columns
    = link_to tool.url, tool.url
    |  by&nbsp;
    = link_to tool.metadata['owner_login'], tool.metadata['owner_url']
    .show-for-small-only
      span.stargazers_count
        i.fa.fa-star
        = tool.metadata['stargazers_count']
      span.forks_count
        i.fa.fa-code-fork
        = tool.metadata['forks_count']

  .small-4.medium-2.medium-push-5.large-2.large-push-5.columns.hide-for-small-only
    span.stargazers_count
      i.fa.fa-star
      = tool.metadata['stargazers_count']
    span.forks_count
      i.fa.fa-code-fork
      = tool.metadata['forks_count']


  .small-12.medium-5.medium-pull-2.large-5.large-pull-2.columns
    - tool.tags.each do |tag|
      span.round.label= link_to tag.name, tag_path(tag.name)